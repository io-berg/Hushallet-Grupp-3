import { MD3LightTheme } from "react-native-paper";

const theme = {
  ...MD3LightTheme,
  roundness: 10,
  colors: {
    ...MD3LightTheme.colors,
    primary: "#000",
    accent: "#ffff",
  },
};

export default theme;
